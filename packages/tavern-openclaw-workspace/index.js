import { definePluginEntry } from 'openclaw/plugin-sdk/plugin-entry';

export default definePluginEntry({
    id: 'tavern-workspace',
    name: 'Tavern Workspace',
    description: 'First-party Tavern workspace integration.',
    register() {},
});
