export const tavernPluginIds = ['merchbase', 'google', 'browser'] as const;

export const tavernPluginCapabilityIds = [
    'plugin.merchbase',
    'plugin.google.calendar',
    'plugin.browser',
] as const;
