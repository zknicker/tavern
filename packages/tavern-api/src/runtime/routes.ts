export const agentRuntimeMutationHeaders = {
    origin: 'x-tavern-origin',
} as const;

export const agentRuntimeMutationOrigins = {
    tavern: 'tavern',
    agentRuntime: 'agent-runtime',
} as const;

export const agentRuntimeRoutes = {
    agent: (id: string) => `/agents/${id}`,
    agentConfig: (id: string) => `/agents/${id}/config`,
    agentFile: (agentId: string, path: string) =>
        `/agents/${agentId}/files/${encodeURIComponent(path)}`,
    agentFiles: (agentId: string) => `/agents/${agentId}/files`,
    agentModel: (id: string) => `/agents/${id}/model`,
    agentName: (id: string) => `/agents/${id}/name`,
    agentThinkingDefault: (id: string) => `/agents/${id}/thinking-default`,
    agents: '/agents',
    capabilities: '/capabilities',
    capability: (id: string) => `/capabilities/${encodeURIComponent(id)}`,
    capabilityRefresh: (id: string) => `/capabilities/${encodeURIComponent(id)}/refresh`,
    capabilitiesRefresh: '/capabilities/refresh',
    update: '/update',
    updateRestart: '/update/restart',
    updateStatus: '/update/status',
    macApps: '/mac-apps',
    modelAccess: '/model-access',
    modelAccessOpenAiSettings: '/model-access/openai',
    modelAccessOpenRouterSettings: '/model-access/openrouter',
    job: (slug: string) => `/jobs/${encodeURIComponent(slug)}`,
    jobRun: (slug: string) => `/jobs/${encodeURIComponent(slug)}/run`,
    jobs: '/jobs',
    openClawConfig: '/openclaw-config',
    cronJob: (id: string) => `/cron-jobs/${id}`,
    cronJobs: '/cron-jobs',
    cronJobRun: (id: string) => `/cron-jobs/${id}/run`,
    cronJobRuns: (id: string) => `/cron-jobs/${id}/runs`,
    cronRun: (id: string) => `/cron-runs/${id}`,
    cronRuns: '/cron-runs',
    events: '/events',
    chatSocket: '/chat',
    highlights: '/highlights',
    chatMessages: (chatId: string) => `/openclaw/chats/${chatId}/messages`,
    chats: '/openclaw/chats',
    cortexBacklinks: (slugOrId: string) =>
        `/cortex/pages/${encodeURIComponent(slugOrId)}/backlinks`,
    cortexCapture: '/cortex/capture',
    cortexEdit: '/cortex/edit',
    cortexGraph: (slugOrId: string) => `/cortex/pages/${encodeURIComponent(slugOrId)}/graph`,
    cortexHistory: (slugOrId: string) => `/cortex/pages/${encodeURIComponent(slugOrId)}/history`,
    cortexIngest: '/cortex/ingest',
    cortexImport: '/cortex/import',
    cortexDreamReport: (id: string) => `/cortex/dream-reports/${encodeURIComponent(id)}`,
    cortexDreamReports: '/cortex/dream-reports',
    cortexPage: (slugOrId: string) => `/cortex/pages/${encodeURIComponent(slugOrId)}`,
    cortexPages: '/cortex/pages',
    cortexRecall: '/cortex/recall',
    cortexRevert: (slugOrId: string) => `/cortex/pages/${encodeURIComponent(slugOrId)}/revert`,
    cortexSchema: '/cortex/schema',
    cortexSchemaAddition: (id: string) => `/cortex/schema-additions/${encodeURIComponent(id)}`,
    cortexSchemaAdditions: '/cortex/schema-additions',
    cortexSearch: '/cortex/search',
    cortexSettings: '/cortex/settings',
    cortexStatus: '/cortex/status',
    health: '/health',
    workspaceAgentInstructions: (agentId: string) =>
        `/workspace/agents/${encodeURIComponent(agentId)}/instructions`,
    workspaceAgentNotes: (agentId: string) =>
        `/workspace/agents/${encodeURIComponent(agentId)}/notes`,
    models: '/models',
    binding: (id: string) => `/bindings/${id}`,
    bindings: '/bindings',
    discordBinding: (id: string) => `/bindings/discord/${encodeURIComponent(id)}`,
    discordBindings: '/bindings/discord',
    skillInstall: '/skills/install',
    skill: (id: string) => `/skills/${id}`,
    skillConfig: (id: string) => `/skills/${id}/config`,
    skills: '/skills',
    sessionGraph: (sessionKey: string) => `/openclaw/sessions/${sessionKey}/graph`,
    sessionMessages: (sessionKey: string) => `/openclaw/sessions/${sessionKey}/messages`,
    sessionPrompt: (sessionKey: string) => `/openclaw/sessions/${sessionKey}/prompt`,
    sessionPreviews: '/openclaw/sessions/previews',
    sessionResync: (sessionKey: string) => `/openclaw/sessions/${sessionKey}/resync`,
    sessions: '/openclaw/sessions',
} as const;
