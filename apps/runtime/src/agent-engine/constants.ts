export const defaultAgentEngineAgentId = 'agt_primary';

// The default agent's display name. Deliberately not "Tavern" — the app is
// Tavern; the assistant living in it needs its own name.
export const defaultAgentDisplayName = 'Otto';
